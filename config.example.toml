[app]
name = "Goer"
env = "local"
debug = true
port = 3000
timezone = "UTC"

[database]
connection = "mysql"

[database.mysql]
host = "127.0.0.1"
port = 3306
database = "goer"
username = "root"
password = ""
max_idle_connections = 10
max_open_connection = 100

[database.sqlite]
database = "database/goer.db"

[paging]
per_page = 15
